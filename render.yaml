services:
  - type: web
    name: play-now
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 3000
    envVars:
      - key: NODE_ENV
        value: production
    publicDir: dist
